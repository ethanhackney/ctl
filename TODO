more comments
linter
document DBUG mode so users can enable it for safe checking
create man page for CTL_ARR
document macros  that can be defined by user
add doc generator for struct def
create array of strings test, the auto generator just does not work for that
do the same for slist hash
